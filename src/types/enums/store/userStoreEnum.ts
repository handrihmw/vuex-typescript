export namespace userStoreEnum {
  export enum Getters {
    GET_USER = 'GET_USER'
  }

  export enum Mutations {
    SET_USER = 'SET_USER',
    SET_LOADING = 'SET_LOADING'
  }

  export enum Actions {
    FETCH_USER = 'FETCH_USER'
  }
}
